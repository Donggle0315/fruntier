document.addEventListener("DOMContentLoaded", function() {
    let loginError = document.getElementById("loginError").innerText;
    if (loginError) {
        alert(loginError);
        alert(loginError);

    }
})

window.history.forward();